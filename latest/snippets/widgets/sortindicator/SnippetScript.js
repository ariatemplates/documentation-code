Aria.tplScriptDefinition({
	$classpath : "ariadoc.snippets.widgets.sortindicator.SnippetScript",
	$prototype : {
		sortByName : function (evt) {
			// no need to do anything
		},
		setToolTip : function (evt) {
			// no need to do anything
		}
	}
});