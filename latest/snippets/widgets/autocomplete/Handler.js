Aria.classDefinition({
	$classpath : "ariadoc.snippets.widgets.autocomplete.Handler",
	$singleton : true,
	$statics : {
		// This is not a handler, it's just a class for unit testing, don't use it!
	}
});