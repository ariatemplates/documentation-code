Aria.tplScriptDefinition({
    $classpath : 'samples.customization.flow.FlowSampleAppScript',
    $prototype : {
        submitSearch : function () {
            alert("in submit Search");
        }
    }
});
